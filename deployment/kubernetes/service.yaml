apiVersion: v1
kind: Service
metadata:
  name: ticketml-api
  namespace: ticketml
  labels:
    app: ticketml-api
spec:
  type: ClusterIP
  selector:
    app: ticketml-api
  ports:
    - name: http
      port: 80
      targetPort: 8000
      protocol: TCP
